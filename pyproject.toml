[project]
name = "bjea-campus-guide-robot"
version = "0.1.0"
description = "Campus guide robot for BJEA, running on Raspberry Pi 5"
requires-python = ">=3.11"

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
